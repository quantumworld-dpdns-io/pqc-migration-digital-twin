schemaVersion: "1.2"

build:
  docker:
    dockerfilePath: Dockerfile
    dockerContext: .

endpoints:
  - name: nginx-ingress
    displayName: PQC Digital Twin — Public API
    service:
      basePath: /
      port: 8080
    type: REST
    networkVisibilities:
      - Public
