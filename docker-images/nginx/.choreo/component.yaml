# Choreo Component Directory MUST be: docker-images/nginx
#
# Manual env (DevOps → Configs & Secrets) OR Architecture Diagram connection:
#   GO_SERVICES_HOST = <go-services project-internal host:port>
#
# Architecture lines appear ONLY after Console → Architecture Diagram → + Create
# (nginx → go-services / gateway). Copy generated resourceRef from UI if codifying.
schemaVersion: "1.2"

endpoints:
  - name: nginx-ingress
    displayName: PQC Digital Twin — Public API
    service:
      basePath: /
      port: 8080
    type: REST
    networkVisibilities:
      - Public
