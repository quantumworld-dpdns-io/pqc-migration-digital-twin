[package]
name = "risk-service"
version = "0.1.0"
edition = "2021"

[dependencies]
risk-engine = { path = "../risk-engine" }
shared-contracts = { path = "../shared-contracts" }
zk-proof = { path = "../zk-proof" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tiny_http = "0.12"
